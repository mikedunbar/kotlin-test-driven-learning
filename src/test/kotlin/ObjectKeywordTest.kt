import org.jetbrains.spek.api.Spek
import org.jetbrains.spek.api.dsl.given
import org.jetbrains.spek.api.dsl.it

/**
 * @author Mike Dunbar
 */
class ObjectKeywordTest : Spek({
    given("the need for a singleton") {
        it("can be done with an object declaration") {
            
        }
    }
})